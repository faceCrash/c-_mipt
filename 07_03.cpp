/*
07.03 (07.12) - Исследование функции test

void test ( Person const & person )
{
    std :: cout << " test : " << person . name () << '\n';
    if ( person . grade () == 10 || person . salary () > 1'000'000)
    {
        save ( Status :: success , person . id () ) ;
    }
    else
    {
        save ( Status :: failure , person . id () ) ;
    }
}

Элементы, приводящие к нормальному ветвлению пути выполнения:
1. Инструкция if-else: В зависимости от истинности или ложности условия, будет
выполнена либо ветка if, либо ветка else.
2. Логическое "ИЛИ" (||) (short-circuit evaluation): Вычисление person.salary()
происходит с использованием коротких схем. Если person.grade() == 10 возвращает
true, то вторая часть условия (person.salary() > 1'000'000) не вычисляется
вовсе, что является неявным ветвлением логики.

Элементы, которые могут привести к генерации исключений (exceptional paths):
1. Операции вывода в std::cout:
   - Оператор << для person.name(), если маска (std::ios_base::exceptions) у
потока настроена на генерацию std::ios_base::failure при ошибках ввода-вывода.
   - Метод вывода '\n' также может выбросить исключение при форматировании или
сбросе буфера.
2. Вызовы методов объекта person: person.name(), person.grade(),
person.salary(), person.id().
   - Так как Person - пользовательский класс, эти методы могут не быть помечены
как noexcept. При их вызове может генерироваться исключение (например, аллокация
памяти при возвращении std::string из name() может выбросить std::bad_alloc).
3. Операторы сравнения (== и >):
   - Если grade() и/или salary() возвращают пользовательский тип, перегруженные
операторы == и > могут генерировать исключения.
4. Вызов функции save():
   - Эта функция вероятно делает какую-то логику сохранения (например, запись в
файл, БД или вызов по сети) и может выбросить исключение, если операция не
удалась.
5. Передача аргументов в save(..., person.id()):
   - Если второй аргумент функции save принимается по значению, а результат
person.id() требует вызова копирующего (или перемещающего) конструктора
пользовательского типа, этот конструктор может выбросить исключение. (Первый
аргумент - Status - это enum, поэтому вряд ли он выкинет исключение).
*/

int main() { return 0; }
